---
"@pagopa/dx-cli": minor
---

Add the `subscription` sub-command to the `init` command, as placeholder

Reserve the `dx init subscription` command for future use, allowing users to scaffold projects with subscription features in upcoming releases.

At the moment the `init subscription` command is hidden from the help menu and does not perform any actions.
