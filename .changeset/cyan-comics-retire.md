---
"nx-release": patch
---

Switch release mode detection from GitHub event type to `.nx/version-plans` directory state on push to main; remove `::notice::` annotations; use `npx nx` for all Nx commands; make PR body generic (no npm reference)
