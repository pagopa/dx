---
"@pagopa/dx-cli": patch
---

The `init` command now creates a local git repository and pushes code to the origin.  
It pushes the README file to the default branch, and pushes the code generated by the scaffolder to a separate branch.
