---
"dx": minor
---

Added a new Agent Skill `github-workflows` to provide guidelines for creating reusable GitHub Actions workflows, focusing on explicit inputs, version pinning, and logic isolation. Also introduced an `AGENTS.md` handbook and new instructions files (`agent-skills.instructions.md`, `typescript-tests.instructions.md`) to standardize AI agent behavior and TypeScript testing practices.

Updated existing guidance, including converting the `github-conventions` documentation into an agent skill and expanding `typescript.instructions.md` with Clean Architecture patterns, functional programming guidance, and naming conventions. Refined `terraform.instructions.md` to clarify formatting expectations and module requirements.

Removed obsolete documentation and configuration, including `.github/copilot-instructions.md`, several outdated ADR files, and `decisions/.prettierrc`, to reduce duplication and keep the decision records and agent guidance current.
