---
"@pagopa/dx-mcpserver": minor
---

Add GitHub OAuth authentication alongside PAT, wiring FastMCP OAuth proxy setup. Fetch OAuth secrets from SSM. Update infrastructure to provision OAuth parameters, IAM policies, and Bedrock reranking support, and bump fastmcp/AWS SDK dependencies.
