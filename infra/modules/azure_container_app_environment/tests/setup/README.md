# setup

Setup module for integration tests of `azure_container_app_environment`.

Reads persistent test infrastructure provisioned by `infra/resources/_modules/testing` and outputs the values needed by integration tests.
