# Azure Managed Redis - Network Access Example

> This example is reserved for the e2e test suite and is currently a placeholder.

It will compare the `default` use case (private networking) and the `development` use case (public networking) side by side, and is wired as the apps fixture consumed by `tests/e2e_test.go`. Tracked as a follow-up under [CES-1909](https://pagopa.atlassian.net/browse/CES-1909).
