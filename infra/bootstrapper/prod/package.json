{
  "name": "prod-bootstrapper",
  "scripts": {
    "format": "terraform fmt",
    "format:check": "terraform fmt -check",
    "tf-plan": "terraform plan -no-color -out=$npm_package_name.tfplan",
    "tf-init": "terraform init",
    "tf-validate": "terraform validate"
  }
}
