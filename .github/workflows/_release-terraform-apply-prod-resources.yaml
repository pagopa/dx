name: Terraform apply on prod resources

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "infra/resources/prod/**"
      - "infra/resources/_modules/**"
      - ".github/workflows/_release-terraform-apply-prod-resources.yaml"
jobs:
  deploy:
    uses: pagopa/dx/.github/workflows/infra_apply.yaml@5dd06bf87a5b37b0c1515b8c4891b53445f723c7 # main
    name: Prod - Deploy
    secrets: inherit
    with:
      environment: prod
      base_path: "infra/resources/"
      override_github_environment: infra-prod
      use_labels: true
      use_private_agent: true
