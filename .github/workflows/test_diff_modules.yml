name: Test Diff Modules

on:
  workflow_dispatch:
  push:
    branches:
      - fix-get-modified-modules

jobs:
  get-modified-modules:
    uses: ./.github/workflows/get_modified_modules.yaml
    name: Get Modified Modules
    with:
      current_branch: ${{ github.event.before }}