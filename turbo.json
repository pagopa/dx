{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
<<<<<<< chores/pr-comment-refactor
      "dependsOn": ["^build"],
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist/**"]
=======
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "src/**/*.ts"
      ],
      "outputs": [
        "dist/**",
        "bin/**"
      ]
>>>>>>> main
    },
    "typecheck": {
      "inputs": ["src/**/*.ts"]
    },
    "lint:check": {
      "inputs": ["src/**/*.ts"]
    },
    "format:check": {},
    "test:coverage": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["coverage/**"]
    }
  }
}
