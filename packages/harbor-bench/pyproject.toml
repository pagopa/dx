[build-system]
# Prevent unreviewed build-backend upgrades from entering the trusted build path.
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "harbor-bench"
version = "0.1.0"
description = "PagoPA Harbor extensions: custom Copilot CLI agent + runtime converter from evals.json to Harbor tasks."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "harbor==0.22.0",
  "jinja2>=3.1",
  "tomli-w>=1.0",
  "pydantic>=2.7",
  "PyYAML>=6.0",
]
classifiers = ["Private :: Do Not Upload"]

[tool.uv]
# Quarantine newly uploaded distributions to leave time for ecosystem review.
exclude-newer = "7 days"
# Stop at the first index containing a package to prevent dependency confusion.
index-strategy = "first-index"
# Reject third-party source distributions instead of executing their build code.
no-build = true
# Make the trusted local project the sole source-build exception to no-build.
no-binary-package = ["harbor-bench"]

[project.scripts]
harbor-bench = "harbor_bench.cli:main"

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
harbor_bench = ["templates/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
