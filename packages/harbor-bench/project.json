{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "harbor-bench",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/harbor-bench",
        "command": "uv run --frozen --package harbor-bench --extra test pytest tests"
      }
    }
  }
}
