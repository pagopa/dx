{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "harbor-copilot",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/harbor-copilot",
        "command": "uv run --frozen --package harbor-copilot --extra test pytest tests"
      }
    }
  }
}
