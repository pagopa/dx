{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Release publish executor",
  "description": "Builds and pushes this release's Docker image from the inferred docker:build target, plus major/major.minor/latest alias tags.",
  "type": "object",
  "properties": {
    "dryRun": {
      "description": "Log the release image tags without building or pushing",
      "type": "boolean"
    }
  }
}
