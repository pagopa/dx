{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Terraform init executor",
  "description": "Initializes Terraform and locks providers and downloaded Registry modules",
  "type": "object",
  "properties": {
    "projectRoot": {
      "description": "Terraform project root used as the working directory",
      "type": "string"
    },
    "args": {
      "description": "Additional arguments passed to terraform init",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "frozenLockfile": {
      "description": "Use Terraform's read-only provider lock validation and fail instead of updating tfmodules.lock.json when downloaded modules differ",
      "type": "boolean",
      "default": false
    }
  },
  "required": ["projectRoot"]
}
