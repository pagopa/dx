backend "s3" {
  bucket         = "{{ awsAccountId }}-terraform-state"
  key            = "{{ repoName }}.repository.tfstate"
  region         = "{{ awsRegion }}"
  dynamodb_table = "terraform-locks"
}
