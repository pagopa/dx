backend "azurerm" {
  resource_group_name  = "{{ repoStateResourceGroupName }}"
  storage_account_name = "{{ repoStateStorageAccountName }}"
  container_name       = "terraform-state"
  key                  = "{{ repoName }}.repository.tfstate"
}
