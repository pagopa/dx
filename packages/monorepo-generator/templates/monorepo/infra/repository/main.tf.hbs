module "github_repository" {
  source  = "pagopa-dx/github-environment-bootstrap/github"
  version = "~> {{ dxGithubEnvironmentBootstrapVersion }}"

  repository = {
    name                   = "{{ repoName }}"
    description            = "{{ repoDescription }}"
    topics                 = []
    reviewers_teams        = []
  }
}
