terraform {
  required_providers {
    github = {
      source  = "integrations/github"
      version = "~> {{ githubTfProviderVersion }}"
    }
  }

  {{> terraformRepositoryBackendState }}
}

# GitHub provider configuration
provider "github" {
  owner = "pagopa"
}
