terraform {
  required_providers {
    github = {
      source  = "integrations/github"
      version = "~> {{ githubTfProviderVersion }}"
    }
  }

  // TODO: Add the backend configuration https://pagopa.atlassian.net/browse/CES-1278
}

# GitHub provider configuration
provider "github" {
  owner = "pagopa"
}
