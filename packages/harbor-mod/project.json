{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "harbor-mod",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/harbor-mod",
        "command": "uv run --frozen --extra test pytest tests"
      }
    }
  }
}
