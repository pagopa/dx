{
  "name": "dx-metrics-import",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "description": "DX Metrics importer that synchronizes GitHub engineering data into PostgreSQL.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pagopa/dx.git",
    "directory": "apps/dx-metrics-import"
  },
  "scripts": {
    "build": "esbuild src/import.ts --bundle --platform=node --format=esm --external:pg --outfile=dist/import.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "import": "tsx src/import.ts"
  },
  "dependencies": {
    "@octokit/auth-app": "^8.2.0",
    "@pagopa/dx-metrics-core": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "js-yaml": "catalog:",
    "octokit": "^5.0.5",
    "pg": "^8.13.0",
    "terrawiz": "^1.0.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@pagopa/eslint-config": "workspace:^",
    "@types/js-yaml": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "esbuild": "^0.28.0",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "nx": {
    "targets": {
      "docker:build": {
        "options": {
          "env": {
            "DOCKER_BUILD_PLATFORMS": "linux/arm64"
          }
        }
      },
      "nx-release-publish": {
        "executor": "nx:run-commands"
      }
    },
    "release": {
      "docker": {
        "repositoryName": "pagopa/dx-metrics-import"
      }
    }
  }
}
