# Generated by harbor-mod. Verifier image for the SEPARATE verifier environment.
#
# The verifier runs in a dedicated container that never receives the agent's
# injected skills, workspace, or agent logs. Harbor transfers to it only the
# task-level `artifacts = [...]` entries (re-materialized at their original
# paths) plus /logs/artifacts/; /logs/verifier/ is bind-mounted for the reward.
FROM ubuntu:24.04

ARG TARGETARCH=amd64

RUN apt-get update && apt-get install -y --no-install-recommends \
    git bash curl ca-certificates python3 python3-pip \
    && rm -rf /var/lib/apt/lists/*

# uv for `uvx --from harbor-rewardkit` in test.sh (judge deps fetched at runtime).
RUN curl -LsSf https://astral.sh/uv/install.sh | sh

COPY test.sh quality.toml /tests/
RUN chmod +x /tests/test.sh

WORKDIR /tests
