{
  "name": "static-test-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "shx mkdir -p dist && shx cp -r public/* dist/"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}