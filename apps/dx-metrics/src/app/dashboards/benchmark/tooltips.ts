/** Tooltip content for the cross-repository Benchmark dashboard. */

export const benchmarkTooltips = {
  benchmark:
    "Compares every configured repository on the same metric. The median is the organisation midpoint; each row shows the distance from it and the relative position.",
  title:
    "Cross-repository benchmark across all configured repositories. Only the time interval affects this view.",
} as const;
