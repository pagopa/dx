---
'@pagopa/azure-tracing': minor
---

Add support for Managed Identity authentication via DefaultAzureCredential. Set APPLICATIONINSIGHTS_USE_MANAGED_IDENTITY=true to authenticate using the resource's managed identity instead of the connection string key. The connection string is still required to identify the Application Insights endpoint.
