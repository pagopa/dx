---
"@pagopa/dx-cli": patch
---

Route the doctor command output through the command presenter, unifying text and JSON output. Text mode now prints a clean per-check summary (without log timestamps) and JSON mode emits the structured result; the exit code still reflects failed checks.
