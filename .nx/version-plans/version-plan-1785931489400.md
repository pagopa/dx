---
self-hosted-runner: patch
---

Make mise user directories writable, rely on repository-managed Node.js, and
reject incomplete upstream runner images. Pre-install a common baseline of
cloud and infrastructure tools through mise.
