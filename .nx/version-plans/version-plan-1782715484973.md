---
'@pagopa/dx-tasks': minor
---

- Add `prComment`, `reportPrComment`, `renderReport` and an improved version of `terraformPlan` task.
- Introduced the `Report` object, with its namespaces and writers/renders
