---
dx-metrics-import: minor
'@pagopa/dx-metrics-core': minor
---

Make the importer incremental by default: persist a per-entity, per-repository cursor in sync_runs.cursor_at and resume from it, so newly added repositories are backfilled without re-downloading data already present. Add --repo to scope a run to one repository. Requires applying the new sync_runs.cursor_at column with drizzle-kit push.
