---
'@pagopa/dx-savemoney': patch
---

Resolve `Pricing lookup failed` warnings caused by logtape misparsing error messages containing `{}` characters, and relax the Retail Prices API schema to accept empty-string metadata fields (e.g. `armSkuName`) that Azure legitimately returns for some meters.
