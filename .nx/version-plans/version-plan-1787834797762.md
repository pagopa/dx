---
self-hosted-runner: minor
---

- Add `aws-cli` and `az-cli`, since the official CSP actions doesn't ship with their binnary
- Add `jq`, which is required by our workflows scripts
- Configure `tflint` and `pnpm` as `mise deps` backends
