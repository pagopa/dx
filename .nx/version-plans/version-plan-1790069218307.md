---
'@pagopa/eslint-config': major
---

Decouple formatting from linting by replacing `eslint-plugin-prettier` with `eslint-config-prettier`.
Prettier is no longer a peer dependency; consumers must configure their formatter independently.
