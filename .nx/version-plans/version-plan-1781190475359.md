---
'@pagopa/dx-cli': patch
---

Ensure `dx add environment` creates missing GitHub environments for non-prod deployments by updating and applying the repository Terraform module before generating the new environment resources. The command now also waits for newly created Key Vault endpoints to become resolvable before writing runner app secrets, avoiding transient initialization failures right after vault creation.
