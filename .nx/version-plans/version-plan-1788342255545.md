---
harbor-bench: minor
harbor-copilot: minor
---

Split the former packages/harbor-bench into apps/harbor-bench (CLI: convert, report, compare) and packages/harbor-copilot (custom Copilot CLI agent + shared metric registry/usage parser). The app depends on the copilot package; the generated config.yaml now references the agent import path harbor_copilot.agents.copilot_cli_mod:CopilotCliMod (breaking: the old harbor_bench.agents path no longer exists).
