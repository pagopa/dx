---
"@pagopa/dx-cli": patch
---

- Add `--source <advisor|custom|all>` flag to `dx savemoney` (default: `all`).
- Separate analysis and report generation to match the updated package API.
- Add a progress spinner on stderr while analysis runs.
