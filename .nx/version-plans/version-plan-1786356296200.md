---
'@pagopa/nx-terraform-plugin': patch
---

Fix `plan` and `publish` targets failing with "Could not resolve ./dist/executors/.../*.js" when the plugin's compiled output is missing, by making both targets depend on the plugin's own `build` target.
