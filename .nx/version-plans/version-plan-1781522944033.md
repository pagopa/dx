---
dx-metrics-import: patch
dx-metrics: patch
---

Keep the DX Metrics Docker builds on each package's existing build script so the image build does not re-enter pnpm workspace resolution inside a reduced Docker workspace.
