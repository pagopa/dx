---
harbor-bench: major
---

Add renderer-neutral comparison reports with comparable-task verdicts, explicit incomplete-trial reporting, per-skill source links, and Markdown, HTML, or JSON output for both diff and compare. BREAKING CHANGE: render_json now returns serialized JSON text; use json.loads(render_json(document)) when structured data is needed.
