---
azure_core_infra: patch
---

Set the required three availability zones on the VPN gateway public IPs so the module converges on the zone-redundant layout required by the v4 VPN gateway.

This change can replace the VPN public IPs and the gateway in stacks that still need to converge, so apply it during a maintenance window.
