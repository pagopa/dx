---
"@pagopa/dx-cli": patch
---

The federated credentials for the bootstrapper identity are now named with the pattern `${repoName}-${githubEnvironmentName}` to ensure they are unique and compliant with Azure naming rules, avoiding potential conflicts and issues with special characters in repository names.
