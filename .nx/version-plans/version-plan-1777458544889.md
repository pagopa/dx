---
"@pagopa/dx-cli": minor
---

DX CLI bootstrap initialization now creates the bootstrapper environment secrets GH_APP_ID, GH_APP_INSTALLATION_ID, GH_APP_KEY, and ARM_TENANT_ID.
The bootstrapper workflow reads the GitHub App credentials from the new GH_APP_* secret names.
