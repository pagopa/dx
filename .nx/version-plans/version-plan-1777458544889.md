---
"@pagopa/dx-cli": minor
---

DX CLI bootstrap initialization now creates the bootstrapper environment secrets GH*APP_ID, GH_APP_INSTALLATION_ID, GH_APP_KEY, and ARM_TENANT_ID.
The bootstrapper workflow reads the GitHub App credentials from the new GH_APP*\* secret names.
