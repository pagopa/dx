---
"@pagopa/nx-dx-docker-plugin": minor
---

Add Nx plugin that infers Docker build/push/package targets for projects with
a Dockerfile, computing image names and release tags without relying on the
official `@nx/docker` release configuration.
