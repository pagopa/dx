---
'@pagopa/dx-cli': patch
---

Add `onlyBuiltDependencies` block to `pnpm-workspace.yaml` file to prevent warning during `pnpm install` operation.

We want to allow the execution of postinstall script of `nx`.
